; Castopod-only ActivityPub interop compatibility shim.
; The prepended file defines only create_actor_from_uri(); Castopod's stock
; helper uses function_exists guards, so every other helper remains upstream.
auto_prepend_file=/interop/castopod/actor_compat.php
