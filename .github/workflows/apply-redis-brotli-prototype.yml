name: Apply Redis Brotli Prototype Patch

on:
  push:
    branches:
      - agent/redis-stream-brotli-compression-prototype
    paths:
      - '.github/workflows/apply-redis-brotli-prototype.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/redis-stream-brotli-compression-prototype
          fetch-depth: 0

      - name: Apply deterministic integration patch
        run: python3 scripts/apply-redis-brotli-prototype.py

      - name: Remove one-shot patch machinery
        run: |
          rm scripts/apply-redis-brotli-prototype.py
          rm .github/workflows/apply-redis-brotli-prototype.yml

      - name: Commit integration
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add fedify-sidecar/src/queue/sidecar-redis-queue-core.ts scripts/apply-redis-brotli-prototype.py .github/workflows/apply-redis-brotli-prototype.yml
          git commit -m 'Wire Brotli codec into Redis Stream queue boundaries'
          git push origin HEAD:agent/redis-stream-brotli-compression-prototype
